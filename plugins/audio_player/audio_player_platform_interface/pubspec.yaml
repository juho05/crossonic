name: audio_player_platform_interface
description: Audio player platform interface
version: 0.0.1
publish_to: "none"

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  rxdart: ^0.28.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
