name: audioplayers_android
publish_to: "none"
version: 0.0.0

flutter:
  plugin:
    platforms:
      android:
        dartPluginClass: AudioplayersPluginAndroid

dependencies:
  audioplayers_platform_interface: ^7.1.1
  flutter:
    sdk: flutter

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"
